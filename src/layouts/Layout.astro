---
const title = Astro.props?.title || 'Hitlift';
const lang = Astro.props?.lang || 'el';

import Header from '../components/Header.astro';
import Footer from '../components/Footer.jsx';
import BackgroundSketch from '../components/BackgroundSketch.astro';
import { createServerClientReadOnly } from '../lib/createServerClient';

const supabase = createServerClientReadOnly(Astro.cookies);
const { data: { user } } = await supabase.auth.getUser();
---

<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet" />
  </head>

  <body class="bg-[#f4f3f0] text-[#1a1a1a] leading-relaxed relative overflow-x-hidden min-h-screen">
    <Header lang={lang} user={user} />

    <!-- Σωστό z-index ώστε να φαίνονται τα σπίτια -->
    <div class="absolute top-0 left-0 w-full h-auto z-0 pointer-events-none select-none">
      <BackgroundSketch />
    </div>

    <main class="relative z-10 pt-[10vh]">
      <slot />
    </main>

    <Footer client:load lang={lang} />
  </body>
</html>
