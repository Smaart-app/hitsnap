---
import { createServerClient } from '@/lib/createServerClientAstro'
import type { APIContext } from 'astro'

export async function get({ cookies, request, redirect }: APIContext) {
  const supabase = createServerClient(cookies, request)
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    return redirect('/en/login')
  }

  return {}
}
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>This space is yours.</title>
    <meta name="description" content="Minimal creative template – Hitsnap" />
    <meta name="robots" content="noindex, nofollow" />
    <style>
      * {
        box-sizing: border-box;
      }

      html, body {
        margin: 0;
        padding: 0;
        background: #000;
        color: #f5f5f5;
        font-family: system-ui, sans-serif;
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .logo {
        max-width: 200px;
        width: 100%;
        height: auto;
        margin-bottom: 2rem;
        border-radius: 6px;
      }

      .tagline {
        font-size: 1.5rem;
        letter-spacing: 0.5px;
        color: #e0e0e0;
      }
    </style>
  </head>
  <body>
    <img src="/preview-logo.png" alt="Logo" class="logo" />
    <div class="tagline">This space is yours.</div>
  </body>
</html>
