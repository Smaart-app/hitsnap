---
import Layout from '../layouts/Layout.astro';
import { createServerClientWithCookies } from '../lib/createServerClient';

let error = false;
let emailAttempted = '';

if (Astro.request.method === 'POST') {
  const formData = await Astro.request.formData();
  const email = formData.get('email');
  const password = formData.get('password');
  emailAttempted = email;

  const supabase = createServerClientWithCookies(Astro.cookies);

  const { data, error: authError } = await supabase.auth.signInWithPassword({
    email,
    password,
  });

  if (authError || !data.session) {
    error = true;
  } else {
    const { session } = data;

    Astro.cookies.set('sb-access-token', session.access_token, {
      path: '/',
      httpOnly: true,
      maxAge: 60 * 60 * 12, // 12 ώρες
    });

    Astro.cookies.set('sb-refresh-token', session.refresh_token, {
      path: '/',
      httpOnly: true,
      maxAge: 60 * 60 * 24 * 30, // 30 μέρες
    });

    return Astro.redirect('/el/blog/editor');
  }
}
---

<Layout title="🔐 Login">
  <main class="max-w-md mx-auto mt-24 p-6 bg-white rounded shadow">
    <h1 class="text-2xl font-bold mb-4">🔐 Login to Admin</h1>

    {error && (
      <p class="text-red-600 mb-4">⚠️ Wrong email or password. Try again.</p>
    )}

    <form method="POST" action="/login" class="space-y-4">
      <div>
        <label class="block font-semibold mb-1">Email</label>
        <input
          type="email"
          name="email"
          class="w-full p-2 border rounded"
          placeholder="anna@hitlift.com"
          value={emailAttempted}
          required
        />
      </div>

      <div>
        <label class="block font-semibold mb-1">Password</label>
        <input
          type="password"
          name="password"
          class="w-full p-2 border rounded"
          placeholder="••••••••••"
          required
        />
      </div>

      <button
        type="submit"
        class="bg-[#50c7c2] text-white px-6 py-2 rounded hover:bg-[#3db2b0] transition font-semibold"
      >
        Login
      </button>
    </form>
  </main>
</Layout>
