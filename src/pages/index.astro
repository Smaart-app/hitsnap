---
export const prerender = false;

const langFromHeader = Astro.request.headers.get('accept-language') || '';
const isGreek = langFromHeader.toLowerCase().startsWith('el') || langFromHeader.toLowerCase().includes('el-gr');
const lang = isGreek ? 'el' : 'en';

return Astro.redirect(`/${lang}`);
---
