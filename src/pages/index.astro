---
export const prerender = false;

const supportedLangs = ['el', 'en'];
const browserLang = Astro.request.headers.get('accept-language')?.split(',')[0]?.slice(0, 2) || 'en';
const lang = supportedLangs.includes(browserLang) ? browserLang : 'en';

Astro.redirect(`/${lang}`);
---