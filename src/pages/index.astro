---
const lang = (Astro.request.headers.get("accept-language") || "en").slice(0, 2);
const supported = ["el", "en"];
const redirectLang = supported.includes(lang) ? lang : "en";
Astro.redirect("/" + redirectLang);
---
